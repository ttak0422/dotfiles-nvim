-- shared ----------------------------------------------------------------------

-- environment variables
-- e.g. vim.env.FOO = "foo"

-- `FILE_NAME` replace to filename
vim.g.sonic_license_header = nil

-- Java ------------------------------------------------------------------------
vim.g.idea_format = nil
-- "/google_checks.xml" | "/sun_checks.xml" | custom config
vim.g.checkstyle = nil
--------------------------------------------------------------------------------
