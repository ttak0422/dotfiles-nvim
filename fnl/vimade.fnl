((. (require :vimade) :setup) {:blocklist {:default {:buf_opts {:buftype [:prompt
                                                                          :terminal]}
                                                     :win_config {:relative true}}}
                               :recipe {:minimalist {:animate true}}})
